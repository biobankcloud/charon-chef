# define the label used to choose the corresponding file locations in the system. 
# You can use Variables defined in charon.config file

DEFAULT_LOCATION=<%= node[:charon][:locations][:default_location] %>

<%= @coc %>

<%= @cloud %>

local=privateRep{cache_{CLIENT_ID}/privateRep}

#external=externalRep{hopsfs:<%= node[:charon][:hdfs][:repo_dir] %>}

